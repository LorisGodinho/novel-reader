# 📊 Diagrama de Classes - Novel Reader

## 🎯 Legenda

```
├─ Herança
→  Associação
◆  Composição
◇  Agregação
```

---

## 📐 Diagrama Completo

```
┌─────────────────────────────────────────────────────────────────┐
│                     CAMADA DE APRESENTAÇÃO                       │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────────────────┐
│      <<class>>               │
│      TemaEscuro              │
├──────────────────────────────┤
│ + BG_PRINCIPAL: str          │
│ + BG_SECUNDARIO: str         │
│ + ACCENT_PRIMARY: str        │
│ + TEXT_PRIMARY: str          │
├──────────────────────────────┤
│ + aplicar_tema(root)         │
└──────────────────────────────┘

┌──────────────────────────────────────────┐
│          <<class>>                       │
│          MusicaFundo                     │
├──────────────────────────────────────────┤
│ - canal_musica: Channel                  │
│ - musica_normal: Sound                   │
│ - musica_combate: Sound                  │
│ - volume_musica: float                   │
│ - mutado: bool                           │
├──────────────────────────────────────────┤
│ + __init__()                             │
│ + carregar_musicas()                     │
│ + tocar_normal()                         │
│ + tocar_combate()                        │
│ + mutar(mutar: bool)                     │
│ + set_volume(volume: float)              │
│ + parar()                                │
└──────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│              <<class>>                                 │
│              NovelReaderGUI                            │
├────────────────────────────────────────────────────────┤
│ - root: Tk                                             │
│ - leitor: LeitorNovel                      ◆           │
│ - musica: MusicaFundo                      ◆           │
│ - engine: EngineNarracaoSimples            ◇           │
│ - narrando: bool                                       │
│ - pausado: bool                                        │
│ - capitulo_atual: int                                  │
│ - paragrafo_atual: int                                 │
│ - conteudo_capitulo: List[str]                         │
│ - modo_compacto: bool                                  │
├────────────────────────────────────────────────────────┤
│ + __init__(root: Tk)                                   │
│ + criar_interface()                                    │
│ + criar_cabecalho()                                    │
│ + criar_secao_controles()                              │
│ + criar_area_visualizacao()                            │
│ + criar_controles_playback()                           │
│ + criar_tooltip(widget, texto)                         │
│ + ajustar_layout_responsivo()                          │
│ + on_resize(event)                                     │
│ + toggle_narracao()                                    │
│ + loop_narracao()                                      │
│ + parar_narracao_completa()                            │
│ + atualizar_display(texto)                             │
│ + atualizar_status()                                   │
│ + precarregar_proximo_paragrafo()                      │
│ + carregar_capitulo(numero: int): bool                 │
│ + paragrafo_anterior()                                 │
│ + paragrafo_proximo()                                  │
│ + capitulo_anterior()                                  │
│ + capitulo_proximo()                                   │
│ + salvar_progresso()                                   │
│ + carregar_progresso()                                 │
└────────────────────────────────────────────────────────┘
                        │
                        │ usa
                        ↓
┌─────────────────────────────────────────────────────────────────┐
│                      CAMADA DE NEGÓCIO                           │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────┐
│          <<class>>                       │
│          LeitorNovel                     │
├──────────────────────────────────────────┤
│ - caminho_novel: str                     │
│ - metadata: Dict                         │
│ - gerenciador_vozes: GerenciadorVozes  ◇ │
│ - wiki: WikiPersonagens                ◇ │
│ - capitulo_atual: int                    │
│ - posicao_atual: int                     │
├──────────────────────────────────────────┤
│ + __init__(caminho_novel: str)           │
│ + carregar_capitulo(numero: int)         │
│   : Optional[Dict]                       │
│ + listar_capitulos_disponiveis()         │
│   : List[int]                            │
│ + obter_total_paragrafos(numero: int)    │
│   : int                                  │
│ + salvar_progresso(cap: int, par: int)   │
│ + carregar_progresso(): Tuple[int, int]  │
│ - _carregar_metadata(): Dict             │
└──────────────────────────────────────────┘
         │                    │
         │ usa                │ usa
         ↓                    ↓
┌─────────────────────┐  ┌──────────────────────┐
│   <<class>>         │  │   <<class>>          │
│ GerenciadorVozes    │  │ WikiPersonagens      │
├─────────────────────┤  ├──────────────────────┤
│ - vozes: Dict       │  │ - caminho: str       │
│ - mapeamento: Dict  │  │ - personagens: Dict  │
├─────────────────────┤  ├──────────────────────┤
│ + associar_voz()    │  │ + obter_info()       │
│ + obter_voz()       │  │ + adicionar()        │
└─────────────────────┘  └──────────────────────┘

┌──────────────────────────────────────────┐
│          <<class>>                       │
│       ProcessadorEmocoes                 │
├──────────────────────────────────────────┤
│ + EMOCOES: Dict[str, Dict]               │
│ + TAGS_MAPEAMENTO: Dict[str, str]       │
│ - deteccao_ativa: bool                   │
├──────────────────────────────────────────┤
│ + __init__()                             │
│ + detectar_emocoes(texto: str)           │
│   : List[Tuple]                          │
│ + processar_texto_com_emocoes(texto)     │
│   : List[Tuple]                          │
│ + aplicar_emocao(config: Dict)           │
│ + ativar_deteccao_auto()                 │
│ + desativar_deteccao_auto()              │
└──────────────────────────────────────────┘

┌──────────────────────────────────────────┐
│          <<class>>                       │
│      GerenciadorVozesMulti               │
├──────────────────────────────────────────┤
│ - mapeamento_personagens: Dict           │
│ - vozes_disponiveis: Dict                │
├──────────────────────────────────────────┤
│ + associar_personagem_voz()              │
│ + obter_voz_personagem(nome: str): str   │
│ + detectar_dialogos(texto: str)          │
│   : List[Tuple]                          │
│ + limpar_mapeamentos()                   │
└──────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                      CAMADA DE SERVIÇOS                          │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────┐
│              <<class>>                               │
│         EngineNarracaoSimples                        │
├──────────────────────────────────────────────────────┤
│ + VOZES: Dict[str, str]                              │
│ - voz_atual: str                                     │
│ - temp_dir: str                                      │
│ - canal: Channel                                     │
│ - volume: float                                      │
│ - velocidade: int                                    │
│ - pausado: bool                                      │
│ - som_atual: Sound                                   │
│ - cache_sounds: OrderedDict                          │
│ - max_cache_size: int                                │
│ - fila_precarregamento: Queue                        │
│ - thread_precarregamento: Thread                     │
│ - precarregamento_ativo: bool                        │
├──────────────────────────────────────────────────────┤
│ + __init__(voz: str, canal: Channel)                 │
│ + solicitar_precarregamento(texto: str)              │
│ + narrar(texto: str, callback_pausado)               │
│ + set_velocidade(velocidade: int)                    │
│ + set_volume(volume: float)                          │
│ + pausar()                                           │
│ + despausar()                                        │
│ + parar()                                            │
│ + parar_precarregamento()                            │
│ - _iniciar_thread_precarregamento()                  │
│ - _worker_precarregamento()                          │
│ - _gerar_audio_async(texto: str): str                │
└──────────────────────────────────────────────────────┘
                        │
                        │ usa
                        ↓
                 ┌─────────────┐
                 │  edge-tts   │
                 │   (async)   │
                 └─────────────┘

┌──────────────────────────────────────────┐
│          <<class>>                       │
│        EngineNarracao                    │
│         (Legacy - CLI)                   │
├──────────────────────────────────────────┤
│ + VOZES: Dict[str, str]                  │
│ - voz_atual: str                         │
│ - temp_dir: str                          │
│ - mapeamento_personagens: Dict           │
├──────────────────────────────────────────┤
│ + __init__(voz_padrao: str)              │
│ + trocar_voz(nome_voz: str): bool        │
│ + associar_personagem(personagem, voz)   │
│ + narrar_segmento(texto, config_emocao)  │
│ - _gerar_audio_async(texto, voz, vel)    │
└──────────────────────────────────────────┘

┌──────────────────────────────────────────┐
│       <<abstract class>>                 │
│       ExtratorGenerico                   │
├──────────────────────────────────────────┤
│ - base_url: str                          │
│ - headers: Dict                          │
│ - session: Session                       │
├──────────────────────────────────────────┤
│ + extrair_novel(slug, inicio, fim)       │
│ + extrair_capitulo(url, numero): Dict    │
│ - _limpar_texto(texto: str): str         │
│ - _salvar_capitulo(dados, novel, num)    │
└──────────────────────────────────────────┘
                    △
                    │ herda
                    │
┌──────────────────────────────────────────┐
│          <<class>>                       │
│      ExtratorCentralNovel                │
├──────────────────────────────────────────┤
│ - base_url: str                          │
│   = "https://centralnovel.com"           │
├──────────────────────────────────────────┤
│ + extrair_novel(slug, inicio, fim)       │
│ + extrair_capitulo(url, numero): Dict    │
│ - _extrair_titulo(soup): str             │
│ - _extrair_conteudo(soup): List[str]     │
└──────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                      CAMADA DE CONTROLE                          │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────┐
│          <<class>>                       │
│      ControladorNarracao                 │
│         (CLI - Legacy)                   │
├──────────────────────────────────────────┤
│ - leitor: LeitorNovel                  ◆ │
│ - engine: EngineNarracao               ◆ │
│ - processador: ProcessadorEmocoes      ◆ │
│ - pausado: bool                          │
│ - parando: bool                          │
│ - pulou: bool                            │
│ - direcao_pulo: int                      │
├──────────────────────────────────────────┤
│ + __init__(caminho_novel: str)           │
│ + iniciar()                              │
│ + narrar_capitulo(numero: int)           │
│ + narrar_continuo(inicio: int)           │
│ + trocar_voz()                           │
│ + toggle_deteccao_auto()                 │
│ + listar_capitulos()                     │
│ - _processar_tecla(key)                  │
│ - _mostrar_menu()                        │
└──────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                  ESTRUTURAS DE DADOS                             │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────┐
│       <<dataclass>>                      │
│         Capitulo                         │
├──────────────────────────────────────────┤
│ + numero: int                            │
│ + titulo: str                            │
│ + conteudo: List[str]                    │
└──────────────────────────────────────────┘

┌──────────────────────────────────────────┐
│       <<dataclass>>                      │
│         Metadata                         │
├──────────────────────────────────────────┤
│ + titulo: str                            │
│ + autor: str                             │
│ + total_capitulos: int                   │
│ + genero: str                            │
└──────────────────────────────────────────┘

┌──────────────────────────────────────────┐
│       <<dataclass>>                      │
│         Progresso                        │
├──────────────────────────────────────────┤
│ + capitulo: int                          │
│ + paragrafo: int                         │
│ + tempo_total: float                     │
│ + timestamp: str                         │
└──────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                  RELACIONAMENTOS PRINCIPAIS                      │
└─────────────────────────────────────────────────────────────────┘

NovelReaderGUI ◆──────→ LeitorNovel
NovelReaderGUI ◆──────→ MusicaFundo
NovelReaderGUI ◇──────→ EngineNarracaoSimples
LeitorNovel ◇──────→ GerenciadorVozes
LeitorNovel ◇──────→ WikiPersonagens
EngineNarracaoSimples ──→ edge_tts (external)
EngineNarracaoSimples ──→ pygame (external)
MusicaFundo ──→ pygame (external)
ExtratorCentralNovel ──→ requests (external)
ExtratorCentralNovel ──→ BeautifulSoup (external)

┌─────────────────────────────────────────────────────────────────┐
│                  MULTIPLICIDADES                                 │
└─────────────────────────────────────────────────────────────────┘

NovelReaderGUI "1" ◆───── "1" LeitorNovel
NovelReaderGUI "1" ◆───── "1" MusicaFundo
NovelReaderGUI "1" ◇───── "0..1" EngineNarracaoSimples
LeitorNovel "1" ◇───── "1" GerenciadorVozes
LeitorNovel "1" ◇───── "1" WikiPersonagens
EngineNarracaoSimples "1" ──── "*" pygame.Sound (cache)
ProcessadorEmocoes "1" ──── "*" ConfigEmocao
GerenciadorVozesMulti "1" ──── "*" MapeamentoPersonagem

```

---

## 🔗 Dependências Entre Camadas

```
┌──────────────────────────┐
│  Camada Apresentação     │
│  (GUI)                   │
└────────┬─────────────────┘
         │ depende
         ↓
┌──────────────────────────┐
│  Camada Negócio          │
│  (Leitor, Processadores) │
└────────┬─────────────────┘
         │ depende
         ↓
┌──────────────────────────┐
│  Camada Serviços         │
│  (Engines, Extratores)   │
└────────┬─────────────────┘
         │ depende
         ↓
┌──────────────────────────┐
│  Camada Dados            │
│  (JSON, MP3)             │
└──────────────────────────┘
```

---

## 📦 Pacotes e Módulos

```
novel_reader/
│
├── novel_reader_gui.py         # Módulo principal GUI
│   ├── TemaEscuro
│   ├── MusicaFundo
│   ├── EngineNarracaoSimples
│   └── NovelReaderGUI
│
├── narrador.py                 # Módulo CLI (legacy)
│   └── ControladorNarracao
│
├── src/                        # Pacote de negócio
│   ├── leitor.py
│   │   └── LeitorNovel
│   ├── gerenciador_vozes.py
│   │   └── GerenciadorVozes
│   └── wiki_personagens.py
│       └── WikiPersonagens
│
├── core/                       # Pacote de processamento
│   ├── emocoes.py
│   │   └── ProcessadorEmocoes
│   └── multi_vozes.py
│       └── GerenciadorVozesMulti
│
├── engines/                    # Pacote de engines
│   └── narracao.py
│       └── EngineNarracao
│
└── extratores/                 # Pacote de extração
    ├── template_generico.py
    │   └── ExtratorGenerico
    └── centralnovel.py
        └── ExtratorCentralNovel
```

---

## 🎯 Interfaces Principais

### Interface de Narração
```python
class INarracao:
    def narrar(texto: str, callback_pausado) -> None
    def pausar() -> None
    def despausar() -> None
    def parar() -> None
    def set_velocidade(velocidade: int) -> None
    def set_volume(volume: float) -> None
```

### Interface de Extração
```python
class IExtrator:
    def extrair_novel(slug: str, inicio: int, fim: int) -> None
    def extrair_capitulo(url: str, numero: int) -> Dict
```

### Interface de Leitor
```python
class ILeitor:
    def carregar_capitulo(numero: int) -> Optional[Dict]
    def listar_capitulos_disponiveis() -> List[int]
    def salvar_progresso(cap: int, par: int) -> None
    def carregar_progresso() -> Tuple[int, int]
```

---

**Nota**: Este diagrama representa a estrutura orientada a objetos atual do projeto Novel Reader versão 2.0.

**Última atualização**: Dezembro 2025
